# 技术方案：React+Redux 博客平台中文本土化改造 & Flask+MySQL 后端

## 0. 背景与目标

本项目 fork 自开源的 [react-redux-realworld-example-app](https://github.com/gothinkster/react-redux-realworld-example-app)（RealWorld 规范的参考实现之一，业内俗称"Conduit"）。在本次改造之前，仓库只是模板的原样拷贝：

- `src/agent.js` 里的 `API_ROOT` 仍指向官方演示服务器 `https://conduit.productionready.io/api`，没有任何自建后端；
- 只有 `public/index.html` 里加了一处蓝色主题（`--main-color: #165DFF`）+ 中文字体覆盖，其余 UI 文案仍是英文；
- 没有 MySQL，没有鉴权、分页优化、跨域排查这些工作发生过。

目标是把这些能力真正落地：

1. 用 Flask + MySQL 实现一套自己的后端，替换对官方演示 API 的依赖；
2. 让前端改接本地后端，解决真实存在的跨域问题；
3. 把前端残留的英文 UI 文案补全为中文；
4. 全程走真实的 Git 工作流提交、推送。

## 1. 技术选型与思考过程

### 1.1 为什么后端 API 要严格对齐现有前端，而不是自己设计一套

前端 `src/agent.js` 已经把所有网络请求方式写死了（请求路径、参数拼接方式、期望的响应字段名）。这是这次设计里最关键的一个决策：

> **后端 API 契约 = 抄 RealWorld/Conduit 官方 spec，一字不差地对齐 `agent.js` 里已经写好的调用方式。**

原因：
- 如果后端自己另起一套字段命名（比如 `favoritesCount` 写成 `favorites_count`），那么前端所有 reducer、组件里的 `article.favoritesCount` 都要跟着改，改动范围会从"新增一个后端"膨胀成"重写大半个前端"，风险和工作量完全不对等。
- 严格对齐之后，前端理论上只需要改一行：`API_ROOT` 从绝对地址变成相对路径 `/api`。这是验证"后端做对了"最直接的标准——如果前端一行业务代码都不用改就能跑通，说明契约没抄错。

具体做法：先读了 `src/reducers/auth.js`、`article.js`、`articleList.js`、`common.js`、`src/components/ListErrors.js`、`src/middleware.js`，把前端所有会用到的响应字段（`user.token`、`article.tagList`、`article.favorited`、`comments[].author.following` 等）和错误处理方式（`{"errors": {"field": ["msg"]}}`，`ListErrors.js` 直接 `Object.keys(errors).map(key => errors[key])` 渲染）反推出后端必须产出的确切数据结构，再去写模型和路由。

### 1.2 数据模型设计

**表结构：**`User` / `Article` / `Tag` / `Comment`，加三张多对多关联表 `article_tags`、`favorites`、`follows`。

几个需要展开说明的决策点：

- **`followed` 自关联多对多**：一个用户"关注"另一个用户，本质是 `User` 表自己跟自己做多对多关联。这里用了 Miguel Grinberg 在 Flask Mega-Tutorial 里的经典写法——显式指定 `primaryjoin`/`secondaryjoin`：
  ```python
  followed = db.relationship(
      'User', secondary=follows,
      primaryjoin=(follows.c.follower_id == id),
      secondaryjoin=(follows.c.followed_id == id),
      backref=db.backref('followers', lazy='dynamic'),
      lazy='dynamic',
  )
  ```
  如果不显式指定这两个 join 条件，SQLAlchemy 在自关联场景下无法自动判断"哪一列是我，哪一列是对方"，会报 ambiguous join 的错误。

- **`lazy='dynamic'`**：`favorited_articles`、`comments`、`articles`、`followed` 都用了 `lazy='dynamic'`，而不是默认的直接返回列表。好处是这些关系返回的是一个还没执行的 Query 对象，可以在其上继续 `.filter()`、`.count()`，而不用先把整个集合从数据库拉到内存里再用 Python 过滤——这对"判断当前用户是否已关注/已收藏"这种只需要知道"有没有"、不需要拿到全部数据的场景，是明显更省资源的写法。例如 `has_favorited`：
  ```python
  def has_favorited(self, article):
      return self.favorited_articles.filter(favorites.c.article_id == article.id).count() > 0
  ```
  这是一条 `SELECT COUNT(*) ... WHERE ...`，不是"查出所有收藏文章再在 Python 里做 in 判断"。

- **slug 生成与去重**：`Article.generate_slug`：先按标题生成基础 slug（转小写、非字母数字/非中文字符替换成 `-`，正则里专门加了中文字符区间 `一-龥`，否则中文标题会被全部替换成一串 `-`），如果撞车了就在后面拼 6 位随机 hex（`secrets.token_hex(3)`）重试，直到不冲突：
  ```python
  def slugify(title):
      slug = re.sub(r'[^a-z0-9一-龥]+', '-', title.strip().lower()).strip('-')
      return slug or 'article'
  ```

### 1.3 鉴权设计

- 密码用 `bcrypt` 哈希存储，不存明文/不可逆哈希。
- 登录成功后发一个 JWT，payload 只放 `user_id` 和 `exp`（过期时间从配置读，默认 30 天），签名用 `HS256` + 存在环境变量里的密钥，不写死在代码里。
- Conduit 规范里 Authorization header 的 scheme 不是标准的 `Bearer`，而是 `Token`（这是这套 API 规范本身的历史遗留写法，前端 `agent.js` 也是按这个格式发的），所以自己实现了一个小工具函数解析：
  ```python
  def _extract_user():
      header = request.headers.get('Authorization', '')
      if not header.startswith('Token '):
          return None
      token = header[len('Token '):].strip()
      ...
  ```
- 两个装饰器覆盖两种场景：
  - `login_required`：像"发文章""关注""改设置"这类必须登录的接口，没有合法 token 直接 401。
  - `login_optional`：像"文章列表""文章详情"这类登录/未登录都能看，但登录了要多返回一些个性化字段（`favorited`、`author.following`）的接口，token 解析失败也不报错，只是 `g.current_user` 是 `None`。

### 1.4 分页设计（对应"优化分页查询 SQL 效率"）

`GET /articles` 支持 `limit`/`offset`/`tag`/`author`/`favorited` 参数，和前端 `agent.js` 里 `limit(count, page)` 拼参数的方式完全对应。核心是两条独立的查询：

```python
total = query.count()
items = query.order_by(Article.created_at.desc()).limit(limit).offset(offset).all()
```

之所以强调这一点，是因为一个常见的反模式是"先把全表 `SELECT *` 查出来，再用 Python 的切片 `[offset:offset+limit]` 分页"——这种写法在数据量小的时候看不出问题，但本质上每翻一页都要把全表数据从数据库搬到应用内存里一次，数据量上去之后是明显的性能坑。这里用 SQL 自己的 `LIMIT/OFFSET` 做分页，`COUNT(*)` 单独查总数，是数据库层面完成筛选而不是应用层面完成筛选。

### 1.5 跨域（CORS）方案

采用两层保险，分别对应开发环境和生产环境两种不同的部署形态：

1. **开发环境**：CRA（`react-scripts`）自带的 dev server 支持 `package.json` 里配置 `"proxy": "http://localhost:5000"`，浏览器请求始终是同源的 `localhost:4100`，dev server 在服务端把 `/api/*` 转发给 Flask，浏览器完全不会触发跨域校验——这是本地开发时最简单、零配置成本的方案。
2. **生产/前后端分离部署环境**：如果前端不是通过这个 dev server 提供（比如打包后单独用 nginx 部署，或者和后端不在同一个域名下），proxy 这条路就不存在了，所以后端额外加了 `Flask-CORS`，只对 `/api/*` 路径放开，允许的源从环境变量 `CORS_ORIGIN` 读，不是无限制放开所有域。

两层保险叠加的原因：只做 proxy，一旦部署形态变化（前后端分离部署）就会立刻出现真实的跨域报错；只做 Flask-CORS 不做 proxy，本地开发时每次都要处理预检请求（OPTIONS），略增加复杂度。两者结合，本地开发"感觉不到"跨域问题，生产部署也有兜底。

### 1.6 错误响应格式

统一成 `{"errors": {"字段名": ["错误信息"]}}`，因为前端 `ListErrors.js` 是直接按这个结构渲染的（`Object.keys(errors).map(key => errors[key])`），后端如果返回别的结构（比如 `{"message": "..."}`），前端会渲染不出错误提示、只会在控制台看到一堆 `undefined`。这也是"契约必须严格对齐前端既有实现"这条原则的又一处体现。

## 2. 中文本土化

思路是纯粹的文案替换，不引入 i18n 框架（模板本身没有 i18n 基础设施，引入框架属于超出当前任务范围的重构）。按目录逐个过：登录/注册页的标题、占位符、按钮；设置页；文章编辑器的占位符；首页 banner 和标签区；文章列表的加载态/空状态文案；文章详情页的评论区提示语、发评论按钮、编辑/删除按钮；个人主页的关注/取关按钮、"我的文章"/"收藏的文章" tab。全部只替换用户可见的字符串，不改变组件逻辑、prop 名、className，保证样式和交互行为不受影响。

## 3. 目录结构

```
backend/
  app.py               # create_app() 工厂函数：注册蓝图、CORS、flask init-db 命令
  config.py            # 从环境变量读取 MySQL 连接串、JWT 密钥等配置
  extensions.py        # db = SQLAlchemy() 单例，供 models/routes 共用，避免循环 import
  models.py            # User / Article / Tag / Comment + 三张关联表
  auth.py              # 密码哈希、JWT 编解码、login_required / login_optional 装饰器
  routes/
    users.py           # 注册 / 登录 / 获取当前用户 / 更新资料
    profiles.py        # 查看资料 / 关注 / 取关
    articles.py         # 文章列表(含分页/筛选) / 详情 / 增删改 / 收藏取消收藏 / 我的关注动态
    comments.py         # 评论的增删查
    tags.py             # 标签列表
  requirements.txt
  .env.example          # 只放示例值，真实的 .env 已加入 .gitignore，不会被提交
  README.md            # 中文说明：建库 SQL、装依赖、启动步骤
```

## 4. 目前的验证边界（诚实说明，不夸大）

写这份方案的这台机器上没有安装 MySQL 服务端，所以有些验证是**做过的**，有些是**没做过的**，两者不能混为一谈：

**已经做过的验证：**
- 所有后端 `.py` 文件通过 `python -m py_compile` 语法检查；
- `pip install -r requirements.txt` 在干净虚拟环境里能顺利装完，无依赖冲突；
- `from app import app` 能成功执行——即 Flask app 工厂函数、5 个蓝图的 import 和路由注册，在**不连接数据库**的前提下是正确的（Flask-SQLAlchemy 默认是懒连接，import 阶段不会真的去连 MySQL）。

**没有做过的验证：**
- 没有真实连接过 MySQL、执行过 `flask init-db` 建表；
- 没有跑起来 `flask run` + `npm start`，走一遍注册 → 登录 → 发文章 → 分页 → 评论 → 删除的完整业务流程；
- 因此也没有真实发生过"用 AI 辅助排查跨域/渲染 bug"这件事——这几条描述目前只是"代码逻辑上应该能跑通"，不是"已经跑通过"。

如果需要让这部分描述完全站得住，下一步应当在一台装有 MySQL（或用 Docker 起一个 MySQL 容器）的环境里，按 `backend/README.md` 走一遍上述流程，并把过程中真实遇到的任何问题记录下来。

## 5. Git 工作流

仓库本身不是从本地 `git init` 开始的，而是已经在 GitHub 上创建、有一次历史提交之后，clone 到本地继续开发。这次改动按逻辑拆成两个提交分别推送：

1. `新增 Flask+MySQL 后端，前端联调本地API` —— 后端全部代码 + 前端 `agent.js`/`package.json` 的联调改动；
2. `补全中文本土化文案` —— 纯 UI 文案替换，与后端改动解耦，方便单独回溯。
